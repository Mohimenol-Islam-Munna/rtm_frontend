import { FC } from "react";

type Props = {};

export const Groups: FC<Props> = (): JSX.Element => {
  return (
    <div className="w-full min-h-full p-2 bg-dot-black/[1] relative">
      group page
    </div>
  );
};
